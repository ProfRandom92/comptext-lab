{
  "title": "Sanitization Summary Contract",
  "description": "Synthetic, machine-readable sanitization summary for confirming that handoff artifacts contain no secrets, raw production logs, customer data, or proprietary documents.",
  "type": "object",
  "required": [
    "source_repo",
    "report_type",
    "synthetic",
    "generated_at",
    "scanned_paths",
    "findings_count",
    "findings_masked",
    "status",
    "notes"
  ],
  "properties": {
    "source_repo": {"type": "string"},
    "report_type": {"type": "string"},
    "synthetic": {"type": "boolean"},
    "generated_at": {"type": "string"},
    "scanned_paths": {"type": "array"},
    "findings_count": {"type": "integer"},
    "findings_masked": {"type": "integer"},
    "status": {"type": "string"},
    "notes": {"type": "string"}
  },
  "additionalProperties": false
}
